'use strict';

module.exports = {
    accessToken: require('./accessToken'),
    client: require('./client'),
    code: require('./code'),
    refreshToken: require('./refreshToken'),
    user: require('./user')
};
